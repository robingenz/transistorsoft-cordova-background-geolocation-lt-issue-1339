import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cordova-background-geolocation-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
